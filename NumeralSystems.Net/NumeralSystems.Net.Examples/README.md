# Executable examples

Run every example from the repository root:

```console
dotnet run --project NumeralSystems.Net/NumeralSystems.Net.Examples -- all
```

Pass `exact`, `periods`, `bits`, or `json` instead of `all` to run one topic.
The examples use project references, so they always exercise the current source tree.
